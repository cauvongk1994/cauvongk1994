#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a, b, c, d;
	printf("nhap so");
	scanf("%d%d%d%d", &a, &b, &c, &d);
	
	printf("toal=%d", a+b+c+d);

	return 0;
}
